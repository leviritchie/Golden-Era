# Golden Era v0.1.310

Campaign map and adventure-object refresh over `v0.1.309`.

## Campaign / Map Payload

- Regenerated and installed all continuity `Story_maps` raw-translation missions (87 maps across RoE Castle, remaining RoE hubs, and AB/SoD).
- Entity substitution updates:
  - Gazebo → Learning Stone
  - Crystals → mana wells
  - Windmill stays on Olden `windmill` (not Wind Rose)
  - Dragon Fly Hive → small creature bank (`black_tower`)
- Footprint-compatibility postpass is fail-open: successful repairs still commit when a few buildings remain unresolved.
- Exact-footprint functional mines stay on the donor 3×3 footprint and sync mesh `generatorConfig` to GATE, so Landon / custom mine visuals no longer drift from the walkable gate.

Unfinished comparison / procedural / layered-atlas test maps remain excluded from the payload.

## Plugin

Ships scrubbed `OfflineUnlockMod` **0.1.310** (release-candidate compile: live probe / AI driver bridge stripped).

## Install

Same thin-installer flow as `v0.1.309`: download one portrait EXE and run it. It downloads the payload parts from this Release.

## Proof Boundary

Generated + installed payload proven for the mapping / footprint / mine ObjectConfig changes. In-game mine visual alignment and broader campaign runtime gameplay remain user-unproven until played.
