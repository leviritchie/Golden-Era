# Golden Era v0.1.307

Superseded by `v0.1.308`.

This tag attempted a PowerShell part-join workaround for GitHub’s 2GB release asset limit. That approach was rejected for end users. Use `v0.1.308` or newer: thin installer EXEs download the payload parts from the GitHub Release automatically.
